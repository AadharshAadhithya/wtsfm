# RF fingerprinting — partial fine-tune
task: rfp
mode: ft2
model: timesfm
checkpoint: null

train_data: /ssd1/aa99435/data/wireless-tsfm/preprocessed/rfp.h5
val_data: null
val_split: 0.2
seed: 0

layer_idx: -1

epochs: 10
batch_size: 256
accum_steps: 1
blr: 1.0e-4
min_lr: 1.0e-7
weight_decay: 0.05
warmup_epochs: 3.0
max_grad_norm: 1.0
smoothing: 0.1

output_dir: tsfm_runs
num_workers: 4
device: cuda
